# Copy to .env and fill in, then link it next to the rendered compose file:
#
#   cp .env.example .env
#   ln -sfn ../../.env pours/deployment/.env
#
# Generate a fresh secret with:  openssl rand -hex 32
SIGNOZ_JWT_SECRET=
