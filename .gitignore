# Clone repo ngoài — có git riêng, không track ở đây
opik/

# Source Foundry tải về chỉ để đọc docs offline, và binary từ GitHub release
signoz/foundry/
signoz/bin/

# File sinh tự động bởi foundryctl, bị ghi đè mỗi lần forge
signoz/pours/

# Sinh boi disk-watch.sh
disk-watch.log
.disk-watch/
